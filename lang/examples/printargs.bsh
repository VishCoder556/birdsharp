#!include "../../code/std.bsh"

int main(long argc, char **argv){
    int i = 0
    while i < cast(int, argc) {
        print(argv[i])
        if ((i + 1) != (cast(int, argc))){
            print(" ")
        }
        i = i + 1
    }
    print("\n")
    exit(0);
    return 0
}
