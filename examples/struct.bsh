#!include "../code/std.bsh"

struct Hello {
    int a
    int b
}

#!define sizeof(struct Hello) = 8;

i32 main(i32 argc, char **argv){
    struct Hello* a = cast(struct Hello*, malloc(sizeof(struct Hello)))
    a.b = 5 // Auto-dereferencing of pointer for memory access
    return a.b
}
